# ============================================
# MINIMAL .env.local FILE
# ============================================

# Production URL
NEXT_PUBLIC_BASE_URL= https://a2itltd.com



# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteBase /
  
#   # Don't rewrite files that exist
#   RewriteCond %{REQUEST_FILENAME} -f
#   RewriteRule ^ - [L]
  
#   # Don't rewrite directories that exist
#   RewriteCond %{REQUEST_FILENAME} -d
#   RewriteRule ^ - [L]
  
#   # For URLs without extension, try to serve the .html version
#   RewriteCond %{REQUEST_FILENAME}.html -f
#   RewriteRule ^(.*)$ $1.html [L]
  
#   # For directories, try to serve index.html inside them
#   RewriteCond %{REQUEST_FILENAME}/index.html -f
#   RewriteRule ^(.*)$ $1/index.html [L]
# </IfModule>
